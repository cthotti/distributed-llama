---
- name: SBCC Team 1 Rock Pi Cluster
  hosts: rocks
  become: true
  vars:
    ansible_ssh_common_args: "-o StrictHostKeyChecking=no"
    # vulkan_gpu_users:
    #   - atjon
  roles:
    # - base
    # - docker
    # - users
    # - lmod
    # - nfs
    # - slurm
    - vulkan
    # - spack
